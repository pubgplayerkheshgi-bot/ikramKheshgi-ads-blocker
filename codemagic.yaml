workflows:
  android-apk:
    name: Build ikramKheshgi APK
    instance_type: linux_x2
    max_build_duration: 30

    scripts:
      - name: Build APK
        script: |
          ./gradlew assembleRelease

    artifacts:
      - app/build/outputs/apk/release/*.apk
